# Initial HTTP-only config for certificate generation
server {
    listen 80;
    server_name _;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 'Waiting for SSL certificates...';
        add_header Content-Type text/plain;
    }
}
